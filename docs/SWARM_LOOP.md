# 蜂群本地闭环：设计基线与缺口清单

本文件是"本地闭环"的**验收基线**，用于避免再次出现"单点功能已实现，但整体闭环被误认为已完成"。

目标链路：

```
用户输入
  → TaskSpec（任务契约）
  → 基因选择
  → PDRI 执行周期（Plan → Do → Review → Integrate）
  → 结果评估
  → 能力沉淀
```

维护规则：每完成一个环节，更新该环节的**当前状态**与**验收条件**；只改受影响的章节，不回写其他文档的历史批次。

对照与取舍见 [REFERENCE_DECISIONS](REFERENCE_DECISIONS.md) D12（TaskSpec）、D13（扇出工具撤回）、D14（蜂群 = 基因库 + RSI 闭环）；代码位置见 [CODE_MAP](CODE_MAP.md)。

---

## 1. 一句话结论

**阶段 A 的骨架已贯通**：TaskSpec、基因定义/存储/选择、**PDRI 周期（显式状态机 + 周期账本）**、**机械评估**、**结果回灌选择** 均已落地并有端到端测试。仍缺的是"更聪明的部分"：独立评审角色、失败蒸馏（distill）、成功归纳（induct）、基因版本/血缘，以及写入门那一片的文件与行数硬预算。

因此当前可以称为：

> TaskSpec → 基因选择 → PDRI 四阶段 → 机械评估 → outcome/gene-less 基线 → 选择器消费历史

还不能称为：

> 具备蒸馏与归纳的自进化蜂群（多角色评审、变异、跨机交换）

**已经可执行的一条测试证明反馈真的闭环**（`test/cycle.test.ts`）：一轮失败被记账后，**下一轮的基因选择确实改变**——失败基因被扣分直至被隔离，另一个候选被选中并成功。

---

## 2. 环节盘点

状态图例：✅ 已实现　⚠️ 部分实现　❌ 未实现

| 环节 | 状态 | 现有实现 | 主要缺口 |
|---|---|---|---|
| TaskSpec 任务契约 | ✅ 基础版 | `src/taskspec.ts`：schema 2；原输入、目标、关键词意图、请求信号、运行时模式、unknowns；`sendTurn` 在写任何日志前生成 | 意图分类较粗；无验收条件字段；约束未全部下发执行层；与评估之间仍无回路（评估只写 outcome，不回写 spec） |
| 基因定义与存储 | ✅ | `src/gene.ts` 铸造不变量与内容寻址；`src/gene-store.ts` 追加式 JSONL、状态=折叠、幂等入库、截断尾容忍；outcome 行含 `status`/`failureClass` | 无基因版本/血缘；无修订与淘汰；无组合基因 |
| 基因选择 | ✅ 基础版（含反馈） | intent 门控 + 信号重叠（词元与中文子串）+ 拉普拉斯平滑成功率 + 新近度（只认最后一次**成功**）+ 连续失败隔离（`quarantineStreak`=2）；零重叠即排除 | 选择理由与候选分数未落盘（只有被选中者进 SendResult）；无探索/利用权衡；失败归因不细分到"基因错还是任务错" |
| PDRI 执行 | ✅ 骨架 | `src/cycle.ts` 纯状态机：`planned→executing→reviewing→integrating→completed`，异常 `failed/cancelled`；顺序错乱抛错、终点不可逆、**评审未通过不得整合**；`src/cycle-store.ts` 追加式 `cycles.jsonl`，状态=对事件日志折叠可重放；runtime 每个 send 一个周期（`cycleId`=`runId`） | 无返工轮次（v1 单趟）；无阶段级文件/行数硬预算（写入门那一片）；无悬空周期定时收口（单进程串行，暂不可能）；Plan 阶段目前= TaskSpec+基因+既有预算事实，没有独立的计划产物 |
| 结果评估 | ✅ 机械版 | `evaluateRun`：从 steps/toolCalls/toolErrors + 失败类别读出 `success/partial/failed/blocked`，证据逐条落盘，`reviewer: "mechanical"`；**不采信模型自报成功**；失败归因区分 budget/model/validation/cancelled/unknown | 没有独立评审者（另一个模型/另一轮）；`objectiveSatisfied` 这类"目标是否达成"的判断仍无机械依据，因此**没有**这个字段；低置信度结果无人工复核入口 |
| 能力沉淀 | ✅ 基础闭环 | outcome 行带四态与失败类别；无基因轮记基线（`address: null`）；表达式折出 attempts/successes/lastSuccessAt/streak；选择器消费这些统计（已测：一次失败即改变下一轮选择） | 无蒸馏（重复失败→guard 草稿）与归纳（无基因轮成功→候选基因）；无增益定价（基线数据已在积累）；无失败档案的独立查询面 |

---

## 3. 逐环节：最终形态与验收条件

### 3.1 TaskSpec

**定位**：后续所有环节共用的任务契约。它明确"这一次要做什么"，不负责执行，也不判定成功。

最终形态：目标 + 意图 + 信号 + 约束 + 风险 + 预算 + 验收条件 + 版本。

验收条件：

- 相同输入得到稳定、可解释的 spec；
- 可被基因选择直接消费（intent 门控、signals 打分）；
- 执行与评估引用同一份 spec（同一 `runId`）；
- 缺目标、约束冲突时可在执行前拒绝，且拒绝**不留痕迹**；
- 硬拒发生在任何写入与模型调用之前。

### 3.2 基因选择

最终形态：候选过滤 → 候选评分（信号匹配、约束匹配、历史成功率、新近度、失败惩罚、探索因子）→ 选择结果（选中基因、候选列表、分数、理由、策略版本）。

验收条件：

- 同一 spec 能列出候选并解释排序；
- 选择理由与候选分数落盘可复查；
- 历史结果影响后续排序；
- 无匹配时明确返回"无基因"，**不伪造选择**；
- 连续失败的基因被降权或隔离。

### 3.3 PDRI 执行

PDRI 是纪律层，不等于"多开几个子 Agent"。

- **Plan**：确认 spec 与选中基因、分解步骤、明确工具与权限、明确预算/超时/取消条件、明确本轮验收标准。
- **Do**：按计划执行模型与工具调用，记录步骤、输入、输出与错误，遵守 M2 边界。
- **Review**：检查目标是否完成、约束是否满足、是否越权、是否需要重试或终止，产出结构化评估。
- **Integrate**：合并结果、写入周期结论、触发能力沉淀、把评估反馈给基因库。

最终形态：每次任务一个显式周期对象，含 `cycleId`、`taskSpec`、选中基因、计划、步骤、工具事件、审查、整合、状态、时间戳。状态只按规则流转：

```
planned → executing → reviewing → integrating → completed
executing/reviewing → failed | blocked | cancelled
```

验收条件：

- 每次任务有唯一周期 ID；
- 四个阶段各有输入、输出与结束原因；
- 取消、超时、失败都能明确收口（含被取代的悬空周期）；
- Review 未通过时**不能**标记完成；
- 只有 Integrate 完成后才进入能力沉淀。

### 3.4 结果评估

最终形态：

```
Evaluation
  ├─ status: success | partial | failed | blocked
  ├─ objectiveSatisfied
  ├─ constraintsSatisfied
  ├─ evidence[]
  ├─ failureClass
  ├─ confidence
  ├─ reviewer
  ├─ retryRecommendation
  └─ notes
```

评估优先使用客观证据：测试是否通过、文件是否存在、工具是否返回成功、输出是否符合 schema、是否越权、是否超预算、是否满足 spec 验收条件。

验收条件：

- 每个周期都有 Evaluation；
- **不依赖模型自报成功**；
- 失败原因可分类；
- 结果可直接更新基因统计；
- 低置信度结果可进入人工复核。

### 3.5 能力沉淀

能力沉淀不等于保存整段对话，而是提取可复用的结构化经验。

最终形态：每周期产出 Outcome（`cycleId`、spec 指纹、选中基因、状态、证据、失败分类、耗时、成本、时间戳）；长期汇总为 GeneStats（`address`、尝试数、成功/部分/失败/阻塞数、成功率、置信度、适用信号、失败模式、最后使用时间）。

验收条件：

- 每次结果关联到基因（或无基因基线）；
- 成败都更新统计；
- **失败不会被沉淀成"成功能力"**；
- 新能力有来源、证据与版本，且经过操作者准入；
- 后续选择消费沉淀结果；
- 可追溯某基因由哪些周期产生。

---

## 4. 与阶段矩阵的关系

| 归属 | 负责内容 |
|---|---|
| M2 权限与受控修改（已收口） | 工具权限、路径保护、写入批准、env 隔离、预算、超时、取消、审计、失败不越权 |
| 蜂群本地闭环（本文件） | TaskSpec、基因库、基因选择、PDRI、结果评估、失败记忆、能力沉淀、历史反馈、基因进化 |
| M6 多 Agent（未开始） | fork、任务调度、并发文件冲突管理、跨 Agent 合并 |

蜂群闭环建立在 M2 之上：基因只能影响**提示与策略**，不能绕过工具权限与批准。M2 收口不等于闭环完成，闭环完成也不等于 M6 多 Agent 完成。

**扇出执行明确后置**：参考 EvoMap 的实验数据（原子拆分 + 隔离上下文 + 程序汇合 70.7%，主从模式仅 38.5%，转述链路丢失 166 个已判对的答案），将来实现扇出时必须是"程序合并、绝不文本回传再综述"，且届时才引入并发（见 D13/D14）。

---

## 5. 外部对照（采用 / 不采用）

| 来源 | 采用 | 不采用（本阶段） |
|---|---|---|
| dsh-swarm（本地源码） | PDRI 显式状态机、纯函数事件转移、终点不可逆、写入门、每周期文件/行数账本、结果回灌、失败/成功双输入、悬空周期收口 | Cordis 宿主、跨进程周期、并行池 |
| EvoMap（研究与 evolver） | 基因=紧凑可进化经验；准入门禁"只从验证成功的轨迹铸造"；反事实增益；防退化骨架（验证命令、固化、回滚）；程序化合并 | GDI 社交评分、能力市场、Validator 共识、跨节点继承（多机专属） |
| WorkBuddy（本地逆向） | 场景关键词路由到能力卡（`curated-experts.json` 的 `{id, scenarios, description, priority}`）；提示词组装分层（能力提示 + 记忆 + 风格 + 模式）；技能包两级（用户级/项目级） | 静态 Skill/Expert 直接等同 Gene；人格与风格层 |

关键分野：**Skill/Expert 是静态手册，Gene 必须带选择依据、执行结果、成功率、失败模式与演化历史**。EvoMap 的负结果（纯蒸馏、没有成功轨迹的基因劣于静态 Skill）正是本项目"铸造必须由操作者把关"的依据。

---

## 6. 实施顺序

### 阶段 A：最小可运行闭环（当前最近一步）

```
TaskSpec → 选择基因 → PDRI 四阶段 → Review → Outcome → 更新基础统计
```

要求：单任务、少量候选、本地存储、显式状态、可审计、可测试、**不自动修改核心能力**。

### 阶段 B：反馈闭环

历史成功率、失败分类与失败记忆、选择理由留档、重新选择、重试与降级建议。

### 阶段 C：能力蒸馏

从重复成功提炼候选基因、从重复失败提炼限制条件、基因版本与血缘、操作者批准后入库。

### 阶段 D：进化优化

探索/利用、基因组合、价值定价、自动降权与淘汰、更复杂的群体协作、（可选）外部能力交换。

**顺序理由**：先把"能进化的骨架"做通，再让它变聪明。跳过 A 直接做 D 会导致无法判断某基因是否真的有效、失败发生在哪一阶段、所谓"群体智慧"是否只是多次调用模型。

---

## 7. 端到端验收

只有同时满足下列条件，才可宣布本地闭环完成：

**任务契约**：输入生成带版本与指纹的 TaskSpec；约束进入执行流程。
**基因选择**：候选可查、选择可解释、结果被记录、无匹配安全退出。
**PDRI**：周期唯一 ID；四阶段均有记录；异常可收口；取消/超时/失败状态明确。
**结果评估**：每周期都有 Evaluation；含客观证据；四态可分；失败可分类。
**能力沉淀**：Outcome 落盘；基因统计更新；失败经验可查；新能力有来源与证据；后续选择消费历史结果。
**安全治理**：工具调用仍受 M2 约束；沉淀不绕过批准；失败不伪造成功；日志不泄漏敏感信息；周期状态与审计一致。

最终还需一条端到端测试证明反馈真的闭环：

```
输入 → TaskSpec → 选择基因 → PDRI → Review → Outcome → GeneStats → 下一次选择发生变化
```

**在这条测试通过之前，不实现自动基因生成、复杂多 Agent 调度与外部能力传播。**

---

## 8. 当前下一步

阶段 A 已完成（1–6 全部落地并测试，含"下一次选择发生变化"的端到端证明）。接着做：

1. **失败档案与蒸馏（`distill`）**：重复失败（含被拒绝的写入）按模式归并，达阈值生成确定性 guard 草稿，仍由操作者决定是否铸造——EvoMap 的负结果要求草稿必须带成功轨迹的证据，不能凭空生成。
2. **成功归纳（`induct`）**：无基因轮的成功轨迹捕获为候选基因草稿（同样过准入门禁）。
3. **写入门与硬预算**：每周期累计文件/行数账本，超预算即拒（D13 已记；此时基因 constraints 才从"记录"变成"机械强制"）。
4. **独立评审角色**：把 `reviewer` 从 `mechanical` 扩展出去（另一个模型或另一轮），并要求评审者不能是执行者。

本文件状态随上述步骤逐条更新。
