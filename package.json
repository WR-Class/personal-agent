{
  "name": "personal-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "build": "tsc --noEmit",
    "start": "node --experimental-strip-types src/cli.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^5.6.3"
  }
}
